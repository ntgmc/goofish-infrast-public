<!-- 此文件由 product/catalog.json 生成，请勿手工编辑。运行 npm run generate:catalog 更新。 -->
# 价格与权益说明

## 版本

| 版本 | 价格 | 核心内容 | 适合用户 |
| --- | ---: | --- | --- |
| 免费预览 | 0 元 | 每 6 小时可提交 2 次完整个人排班。系统有空闲资源时会自动开始，繁忙时可能需要多等一会儿。 | 想先试用、低频使用、不想研究复杂配置的普通玩家 |
| 30 天尝鲜维护包 CDK | 12.9 元 / 30 天 | 30 天内不限次数生成和增量重算，排班优先处理，并可使用 MAA JSON、完整数据和高级分析。 | 近期会练度提升或想先验证完整能力的玩家 |
| 90 天版本维护卡 CDK | 24.9 元 / 90 天 | 90 天内不限次数生成和增量重算，排班优先处理，并可使用 MAA JSON、完整数据和高级分析。 | 一个版本周期内会持续养成和调整方案的玩家 |
| 365 天年度维护卡 CDK | 44.9 元 / 365 天 | 365 天内不限次数生成和增量重算，排班优先处理，并可使用 MAA JSON、完整数据和高级分析。 | 全年会持续养成、希望降低长期维护成本的玩家 |
| 单账号终身卡 CDK | 59 元 / 长期 | 一次购买即可长期不限次数生成和增量重算，排班优先处理，并支持配置切换、MAA JSON、完整数据下载、高级分析、练度建议和自定义配置。 | 长期维护同一账号、会持续研究和重新优化基建方案的玩家 |
| 积分单次排班 | 1000.00 积分/成功主排班 | 高级版单次结果，不含场景对比 | 低频个人用户；按成功任务使用，不锁定长期权益 |
| 个人增量重算 | 700.00 积分/成功重算 | 绑定同一 UID 的已有成功结果，用于练度提高或新增干员后的重新优化 | 已有方案、只需跟进干员数据变化的个人用户 |
| 场景对比包 | 300.00 积分/成功对比 | 一次最多比较 3 个额外基建配置，输出效率指标、差距与适用条件 | 想比较多种基建配置但不需要长期权益的用户 |
| 商用版积分单次 | 1100.00–1350.00 积分/成功主排班 | 标价 1500.00 积分，累计等级自动折扣 | 已获授权处理多个 UID 的服务商 |

## 维护方案选购建议

- 30 天尝鲜维护包：12.9 元 / 30 天，适合近期练度变化较多、想集中验证方案的用户。
- 90 天版本维护卡：24.9 元 / 90 天，适合覆盖一个版本周期并持续调整方案的用户。
- 365 天年度维护卡：44.9 元 / 365 天，适合全年维护同一 UID 的用户。
- 终身卡：59 元 / 长期，长期使用和高频重新优化的用户更划算；所有个人维护方案都只绑定一个游戏 UID。

## 权益口径

- 完整个人排班：根据当前干员和配置生成一份可直接使用的新轮换方案。
- 变化影响预判：对比当前数据与最近一次成功排班，只返回是否建议重新生成、预计收益区间、受影响设施和关键干员，不生成新排班。
- 个人增量重算：绑定同一档案的已有成功结果，重新计算并生成一份完整的新排班；付费 CDK 已包含，个人按次档案按成功任务计费。

## 按次排班规则

- 积分单次排班档案每个网站账号终身最多 1 个，每次成功主排班扣除 1000.00 积分（约 10 元）。
- 个人增量重算每次成功扣除 700.00 积分（约 7 元），必须绑定同一档案的一份已有成功结果；当前版本绑定历史基线并重新执行完整计算，暂未复用底层计算缓存。
- 场景对比包每次成功扣除 300.00 积分（约 3 元），一次最多提交 3 个额外基建配置；已有场景券仍可替代积分包且不会重复消耗。
- 商用版积分单次标价 1500.00 积分；Lv1–Lv4 实扣依次为 1350.00 / 1250.00 / 1150.00 / 1100.00 积分，最低价不低于个人按次价。
- 仅成功且结果已持久化的主排班扣费；排队时预留，失败、取消、队列过期或死信会释放预留。
- 按次档案包含高级版单次结果、MAA JSON、完整计算 JSON、练度建议和 ROI；同一数据快照的格式导出不重复收费，但不开放场景对比实验室或 trusted 优化器选项。
- 商用档案仅可处理已获授权的数据；不得转售 MaaTool 账号或 CDK，档案和积分不可转让。

## 免费预览规则

- 绑定森空岛 UID，一个游戏 UID 全站只能领取一次。
- 每 6 小时最多提交 2 次完整个人排班；同一档案同时最多有 1 个排班正在等待或计算。
- 系统有空闲资源时会自动开始免费排班；繁忙时会先处理付费排班，你的排班会继续等待。
- 如果计算途中系统变得繁忙，你的排班会自动重新排队并重新计算，无需再次提交；完成时间可能会延后。
- 已有成功排班后，每月可进行 2 次变化影响预判；预判不生成新排班，也不占用每 6 小时 2 次的完整排班提交次数。
- 每月预判次数用尽后，可使用一张变化预判券增加 1 次；使用券仍只返回判断结果，不生成新排班。

## 单账号卡账号规则

- 完整个人排班：30 天、90 天和 365 天卡均可在有效期内不限次数生成，终身卡可长期不限次数生成。
- 个人增量重算：所有有效付费 CDK 均已包含，绑定同一档案的已有成功结果后即可不限次数使用。
- 场景对比额度：30 天卡 1 次、90 天卡 3 次、365 天卡 8 次，终身卡无限；每次最多提交 3 个额外基建配置。周期卡额度用完后不能继续提交场景对比，个人按次档案可购买 300.00 积分场景对比包。

- 30 天尝鲜维护包、90 天版本维护卡、365 天年度维护卡和终身卡 CDK 均仅用于一个游戏账号；绑定森空岛 UID 后不支持自行更换。
- 30 天、90 天和 365 天维护包均为一次性预付，不自动续费；终身卡指在服务持续提供且授权正常的前提下长期维护同一账号。
- 早期已按 49 元购买的终身卡继续保留原有长期权益；当前公开终身卡价格为 59 元。
- 干员归属、练度或干员池出现异常变化时可能先拦截；重复异常可能冻结档案。
- 干员数据误拦截需要通过 MaaTool 官方 QQ 群人工核验。
- 个人按次档案仅限本人非商业使用；商用档案仅可处理数据权利人已授权的数据。
- 商用主排班按成功任务计费；MaaTool 账号、CDK、档案和积分均不可转让或转售。
- 30 天、90 天和 365 天 CDK 可在有效期内不限次数生成完整个人排班和使用个人增量重算，终身卡可长期不限次数使用。
- 人工核验材料齐全后，客服将在 2 个工作日内首次响应；工作日按中国大陆法定工作日计算，最终核验与解冻时间视复杂度而定。

## 功能对比

| 功能 | 免费预览 | 30 天尝鲜维护包 CDK | 90 天版本维护卡 CDK | 365 天年度维护卡 CDK | 单账号终身卡 CDK |
| --- | --- | --- | --- | --- | --- |
| 有效期 | 免费预览权益，按页面规则使用 | 30 天 | 90 天 | 365 天 | 长期，不设到期日 |
| 森空岛导入干员数据 | 支持，必须绑定 UID | 支持，可绑定并保存到同一账号工作区 | 支持，可绑定并保存到同一账号工作区 | 支持，可绑定并保存到同一账号工作区 | 支持，可绑定并保存到同一账号工作区 |
| MAA 干员识别文件导入 | 不支持手动上传干员 | 支持，受干员数据风控规则约束 | 支持，受干员数据风控规则约束 | 支持，受干员数据风控规则约束 | 支持，受干员数据风控规则约束 |
| 完整个人游戏内轮换队列 | 每 6 小时可提交 2 次；繁忙时会自动排队等待 | 支持，30 天内不限次数生成 | 支持，90 天内不限次数生成 | 支持，365 天内不限次数生成 | 支持，可长期不限次数生成 |
| 个人增量重算 | 数据变化后可重新生成完整排班 | 支持，30 天内不限次数使用 | 支持，90 天内不限次数使用 | 支持，365 天内不限次数使用 | 支持，可长期不限次数使用 |
| 排班等待 | 有空闲资源时自动开始；繁忙时继续排队 | 优先处理 | 优先处理 | 优先处理 | 优先处理 |
| MAA JSON 下载 | 高级体验期或导出体验券可用 | 支持，仅包含 MAA 执行所需排班数据 | 支持，仅包含 MAA 执行所需排班数据 | 支持，仅包含 MAA 执行所需排班数据 | 支持，仅包含 MAA 执行所需排班数据 |
| 243 均衡 / 243 搓玉 / 333 搓玉预设 | 支持 | 支持 | 支持 | 支持 | 支持 |
| 变化影响预判（不生成新排班） | 已有排班后每月 2 次，返回重新生成建议、收益区间和受影响项；变化预判券可增加 1 次 | 无需单独预判，可直接不限次数增量重算 | 无需单独预判，可直接不限次数增量重算 | 无需单独预判，可直接不限次数增量重算 | 无需单独预判，可直接不限次数增量重算 |
| 练度提升建议与 ROI | 不支持 | 支持 | 支持 | 支持 | 支持 |
| 完整数据与原始结果 | 不支持 | 支持，可独立下载完整计算 JSON | 支持，可独立下载完整计算 JSON | 支持，可独立下载完整计算 JSON | 支持，可独立下载完整计算 JSON |
| 自定义配置 | 不支持 | 支持 | 支持 | 支持 | 支持 |
| 场景对比实验室 / Pareto 高级分析 | 不支持 | 含 1 次，单次最多 3 个额外配置 | 含 3 次，单次最多 3 个额外配置 | 含 8 次，单次最多 3 个额外配置 | 无限次，单次最多 3 个额外配置 |
| 批量报告导出 / 商用授权 | 不支持 | 不支持 | 不支持 | 不支持 | 不支持 |
| 更换游戏账号 | 不支持 | 不支持自行更换；需人工核验 | 不支持自行更换；需人工核验 | 不支持自行更换；需人工核验 | 不支持自行更换；需人工核验 |

## 售后与申诉

- 通过MaaTool 官方 QQ 群提交：账号标识、订单或 CDK 的安全标识、报错时间、不含敏感信息的错误描述和操作步骤。
- 请勿发送：密码、完整 CDK、二维码、Cookie、森空岛凭据。
- 材料齐全后 2 个工作日内首次响应；核验和解冻完成时间视复杂度而定，不承诺固定完成时限。
