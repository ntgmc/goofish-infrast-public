<!-- 此文件由 product/catalog.json 生成，请勿手工编辑。运行 npm run generate:catalog 更新。 -->
# 价格与权益说明

## 版本

| 版本 | 价格 | 核心内容 | 适合用户 |
| --- | ---: | --- | --- |
| 免费预览 | 0 元 | 领取 1 次免费完整个人排班权益，确认期内可修正，锁定后仍可按规则检测是否值得重排。 | 想先试用、低频使用、不想研究复杂配置的普通玩家 |
| 单账号终身版 CDK | 49 元 | 免费预览全部能力，加上长期完整重新生成、轻量 MAA JSON 导出、完整计算数据下载、高级分析、练度建议和自定义配置。 | 想长期维护同一账号基建方案、需要导出或深度自定义的玩家 |
| 普通按次 | 600.00 积分/成功主排班 | 高级版单次结果，不含场景对比 | 低频个人用户；约第 9 次起终身版更划算 |
| 商用按次 | 600–900 积分/成功主排班 | 累计获得 10,000 积分自动解锁，Lv1–Lv4 自动折扣 | 已获授权处理多个 UID 的服务商 |

## 按次排班规则

- 普通按次档案每个网站账号终身最多 1 个，每次成功主排班扣除 600.00 积分。
- 商用标价 1000.00 积分；Lv1–Lv4 实扣依次为 900.00 / 800.00 / 700.00 / 600.00 积分，最低价不低于个人按次价。
- 仅成功且结果已持久化的主排班扣费；排队时预留，失败、取消、队列过期或死信会释放预留。
- 按次档案包含高级版单次结果、MAA JSON、完整计算 JSON、练度建议和 ROI，但不开放场景对比实验室或 trusted 优化器选项。
- 商用档案仅可处理已获授权的数据；不得转售 MaaTool 账号或 CDK，档案和积分不可转让。

## 免费预览规则

- 绑定森空岛 UID，一个游戏 UID 全站只能领取一次。
- 首次领取后拥有 1 个免费完整排班权益；首次完整生成后进入 24 小时确认期，确认期内最多生成 3 次完整方案，总次数包含首次生成。
- 权益锁定后仍可刷新同 UID 的森空岛干员数据、查看历史方案，并且每月可检测是否值得重排 2 次。
- 检测结果为“强烈建议重排”时，当月额外允许 1 次完整免费生成；该生成不再开启新的确认期。

## 单账号终身版账号规则

- 单账号终身版档案仅用于一个游戏账号，可选择森空岛导入或 MAA 干员 JSON 初始化；绑定森空岛 UID 后不支持自行更换。
- 长期更新指在服务持续提供且授权正常的前提下维护同一账号，不表示可以任意换号。
- 干员归属、练度或干员池出现异常变化时可能先拦截；重复异常可能冻结档案。
- 干员数据误拦截需要通过 MaaTool 官方 QQ 群人工核验。
- 个人按次档案仅限本人非商业使用；商用档案仅可处理数据权利人已授权的数据。
- 商用主排班按成功任务计费；MaaTool 账号、CDK、档案和积分均不可转让或转售。
- 人工核验材料齐全后，客服将在 2 个工作日内首次响应；工作日按中国大陆法定工作日计算，最终核验与解冻时间视复杂度而定。

## 功能对比

| 功能 | 免费预览 | 单账号终身版 CDK |
| --- | --- | --- |
| 森空岛导入干员数据 | 支持，必须绑定 UID | 支持，可选绑定并保存到同一账号工作区 |
| MAA 干员识别文件导入 | 不支持手动上传干员 | 支持，受干员数据风控规则约束 |
| 完整个人游戏内轮换队列 | 支持，受免费完整排班权益限制 | 支持，可长期重新生成 |
| MAA JSON 下载 | 高级体验期或导出体验券可用 | 支持，仅包含 MAA 执行所需排班数据 |
| 243 均衡 / 243 搓玉 / 333 搓玉预设 | 支持 | 支持 |
| 检测是否值得重排 | 每月 2 次；强烈建议时当月额外 1 次完整免费生成 | 可直接重新生成并查看完整结果 |
| 练度提升建议与 ROI | 不支持 | 支持 |
| 完整数据与原始结果 | 不支持 | 支持，可独立下载完整计算 JSON |
| 自定义配置 | 不支持 | 支持 |
| 场景对比实验室 / Pareto 高级分析 | 不支持 | 支持，最多比较 24 个场景 |
| 批量报告导出 / 商用授权 | 不支持 | 不支持 |
| 更换游戏账号 | 不支持 | 不支持自行更换；需人工核验 |

## 售后与申诉

- 通过MaaTool 官方 QQ 群提交：账号标识、订单或 CDK 的安全标识、报错时间、不含敏感信息的错误描述和操作步骤。
- 请勿发送：密码、完整 CDK、二维码、Cookie、森空岛凭据。
- 材料齐全后 2 个工作日内首次响应；核验和解冻完成时间视复杂度而定，不承诺固定完成时限。
